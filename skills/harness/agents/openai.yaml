interface:
  display_name: "Harness"
  short_description: "Gated Plan, Implement, Review workflow"
  default_prompt: "Use $harness to plan, implement, review, and complete this change."

policy:
  allow_implicit_invocation: false
